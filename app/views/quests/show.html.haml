.container
  .center-block
    %p クエスト
    = @quest.title
    - if !(@quest.describe.blank?)
      %p クエスト詳細
      = @quest.describe
    %p 難易度
    = @quest.difficulty
    %p 経験値
    = @quest.xp
    ポイント
    - if @quest.public
      %p このクエストは公開されています。
    - else
      %p このクエストは公開されていません。
    - if @quest && @quest.user == current_user
      = link_to "編集する", edit_quest_path(@quest)