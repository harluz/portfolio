%h1 My quest list
%p Find me in app/views/quests/my_quest.html.haml

- @quests.each do |quest|
  %p
  = quest.title
  = link_to "詳細", quest_path(quest)
  - if quest && quest.user == current_user
    = link_to "編集する", edit_quest_path(quest)
    - if quest.public == false
      = link_to "削除する", quest_path(quest), method: :delete
